/*jslint nomen: true*/
(function (window, rJS) {
  "use strict";

  var gk = rJS(window);
  gk.
    ready(function (g) {
    })
    .declareMethod('render', function () {
      return "";
    });
}(window, rJS));
